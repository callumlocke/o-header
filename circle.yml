machine:
  node:
    version: 4.2.2
  post:
    - npm install -g Financial-Times/origami-build-tools#node4
dependencies:
  override:
    - obt install
  cache_directories:
    - "node_modules"
test:
  override:
    #- obt test
    #- obt verify
    - obt demo
    - './node_modules/.bin/http-server':
        background: true
    - sleep 5
    - curl -I http://localhost:8080/
    - curl -I http://localhost:8080/demos/local/header.html
    # - ./node_modules/.bin/pa11y http://localhost:8080/demos/local/header.html
